[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router — serve index.html for all routes so deep links work
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
